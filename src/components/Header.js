import { Component } from "react";
import styles from './Header.module.scss';

export class Header extends Component {

  render() {
    return (
      <header>
        <h1>Movies.TV.Find.Lookfor.Results.Get</h1>
      </header>
    );
  }
}